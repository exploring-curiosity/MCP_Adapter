dedalus-mcp>=0.7.0
dedalus-labs>=0.2.0
# dedalus-mcp asks for mcp>=1.25.0, which now resolves to 2.x. mcp 2.0 dropped
# Cursor from mcp.types, so dedalus_mcp fails to import on a fresh install.
mcp>=1.25,<2
pyyaml>=6.0
jinja2>=3.1
click>=8.1
httpx>=0.28
jsonschema>=4.20
pydantic>=2.12
python-dotenv>=1.0
fastapi