[pytest]
# The generated servers under output/ each contain a test_server.py. Without
# importlib mode pytest derives module names from the basename alone, so the
# second one collides with the first and collection stops.
addopts = --import-mode=importlib

# Every test in this suite is async.
asyncio_mode = auto
